path: ../yolov5-6.0/data/mask  # 数据集根目录dataset root dir
train: train/images  # 训练图片 train images
val: valid/images  # 验证图片val images
test: # test images (optional)

# Classes
nc: 3  # number of classes
names: ['with_mask', 'without_mask', 'mask_weared_incorrect']  # class names
